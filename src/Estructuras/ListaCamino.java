package Estructuras;

public class ListaCamino {
    String inicio;
    String ultimo;

    public ListaCamino() {
        this.inicio = null;
        this.ultimo = null;
    }
    
    /*public void agregarInicio(String recorrido) {
        NodoVia nuevo = new NodoVia(this.getId(), via);
        if(this.getInicio()==null){
            this.setInicio(nuevo);
            this.setUltimo(nuevo);
        }    
        else{
            nuevo.setSig(this.getInicio());
            this.setInicio(nuevo);
        }
        this.setId(id +1);
    }*/
}
